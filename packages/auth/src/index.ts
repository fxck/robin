// Server-side auth
export * from './server/auth';

// Client-side auth (only export types/interfaces, not the client itself to avoid build issues)
// Import from '@robin/auth/client' directly in React components
